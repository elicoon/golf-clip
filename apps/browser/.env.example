# Supabase — required for ML feedback collection (shot/tracer feedback)
# Create a project at https://supabase.com and copy the values from
# Project Settings > API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# The app runs fully without Supabase — feedback features will be disabled
# if these are not set.
