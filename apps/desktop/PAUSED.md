# PAUSED

This directory is not currently in active development.

**Do not:**
- Reference this code for new features
- Make changes here
- Use as examples for the browser app

**Status:** On hold until desktop app work resumes. The production API at golfclip-api.fly.dev uses this code, but active feature development is in the browser app.

**Active development:** See `apps/browser/` instead.
